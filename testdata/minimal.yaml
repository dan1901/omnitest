version: "1"

targets:
  - name: "httpbin"
    base_url: "https://httpbin.org"

scenarios:
  - name: "minimal"
    target: "httpbin"
    vusers: 1
    duration: "1s"
    requests:
      - method: GET
        path: "/get"
